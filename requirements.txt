# ── Core numerics ─────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.10

# ── Deep learning ─────────────────────────────────────────────────
torch>=2.0
transformers>=4.36           # Swin-V2 backbone via AutoImageProcessor / AutoModel

# ── Classical ML ──────────────────────────────────────────────────
scikit-learn>=1.3            # AgglomerativeClustering, KMeans, LedoitWolf, NearestNeighbors

# ── Hyperparameter optimisation ───────────────────────────────────
optuna>=3.4                  # multivariate TPE + Hyperband pruner

# ── Imaging / data utils ──────────────────────────────────────────
Pillow>=10.0
opencv-python>=4.8
tqdm>=4.66

# ── Notebooks (optional, but required to run the .ipynb files) ────
jupyter
ipykernel
matplotlib>=3.7
